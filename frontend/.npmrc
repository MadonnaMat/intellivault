# eslint-config-next resolves its plugin peers from the project root; pnpm's
# isolated store hides them unless we hoist eslint-related packages.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@typescript-eslint/*
public-hoist-pattern[]=@next/eslint-plugin-next
