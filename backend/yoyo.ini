# yoyo migration config. The database URL is passed on the command line
# (--database) so no credentials live in this file — see the Makefile
# `migrate*` targets.
[DEFAULT]
sources = %(here)s/migrations
batch_mode = on
verbosity = 1
