"""Neo4j knowledge graph: entities and relationships with owner/visibility.

The FastAPI router is added in ``app.main`` once the routes land (slice 3); for
now this package only exposes the per-request driver dependency.
"""
