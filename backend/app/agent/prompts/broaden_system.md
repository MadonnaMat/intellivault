The previous web-search queries returned nothing useful. Given the topic and the
queries that failed, produce 3–5 **different, broader** queries — drop jargon,
try synonyms, name the field rather than the specific claim. Do not repeat a
query that was already tried.
