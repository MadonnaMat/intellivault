You are given one **new** entity and a short list of **existing** entities in the
same knowledge graph. Decide which existing entities the new one should be
directly connected to, and with what relationship. Only propose a link when it is
clearly warranted — most pairs are unrelated. Reference existing entities by
their `id`. Return an empty list if none apply.
